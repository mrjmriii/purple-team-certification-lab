id: "12"
name: "Change control bypass"
category: "Social Engineering Drift"
pretext: "Emergency change—no time for ticket"
human_factor: "Urgency + authority"
drift_change:
  description: "Undocumented config change"
  scope: ["host:lab-app"]
assumed_access:
  credentials: "Operator-level"
  network: "Local"
mapped_kill_chain:
  - phase: "Defense Evasion"
    technique: "Impair Defenses (T1562)"
    scripts: ["change_without_ticket.py"]
    telemetry: ["stdout: no-ticket change marker"]
  - phase: "Execution"
    technique: "Command Execution (T1059)"
    scripts: ["config_touch_tmp.py"]
    telemetry: ["file: /tmp/config-change.txt", "stdout: touch marker"]
  - phase: "Impact"
    technique: "Service Disruption Analog"
    scripts: ["impact_marker.py"]
    telemetry: ["stdout: impact marker"]
expected_wazuh_alerts:
  - { name: "No-ticket change", rule_id: "TODO-CHANGE-NO-TICKET", evidence: ["stdout marker"] }
  - { name: "Config touch tmp", rule_id: "TODO-CONFIG-TMP", evidence: ["/tmp/config-change.txt"] }
detection_notes:
  false_positive_considerations: ["Break-glass with doc later"]
  tuning: ["Require ticket marker; time-box"]
runner_parameters:
  timeout_seconds: 150
  artifacts_path: "/tmp/purple-lab-artifacts"
  log_level: "info"
